{
  "name": "memflow-ai-visualizer",
  "version": "2.0.0",
  "description": "Browser-based C learning workspace with execution timeline, memory visualization, multilingual lessons, and AI-assisted explanations",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "validate": "node --check server.js && node --check services/dockerRunner.js && node --check src/authMiddleware.js && node --check src/sessionManager.js && node --check public/index.js && node --check public/lessons-hi.js",
    "test": "npm run validate",
    "docker:build": "docker build -t memflow-ai ."
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "c",
    "compiler",
    "education",
    "visualization",
    "memory",
    "socket.io",
    "aws",
    "bedrock",
    "cognito",
    "hackathon"
  ],
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.893.0",
    "@aws-sdk/client-dynamodb": "^3.893.0",
    "@aws-sdk/lib-dynamodb": "^3.893.0",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
